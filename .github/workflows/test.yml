name: Bump Patch test

on:
  workflow_dispatch

jobs:
  history:
    name: Bump
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Git Repository
        uses: actions/checkout@v3
      - name: Bump
        id: bump
        uses: progovoy/vmn-action@vmna_0.1.17
        with:
          stamp-mode: patch
          app-name: ''
      - run: echo ${{ steps.bump.outputs.verstr }}
