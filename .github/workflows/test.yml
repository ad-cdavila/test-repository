name: Bump Patch test

on:
  workflow_dispatch

jobs:
  history:
    name: Bump
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Git Repository
        uses: actions/checkout@v3
      - name: Bump
        id: bump
        uses: lhstrh/action-repo-semver@v1.1.2
        with:
          bump: patch
      - run: echo ${{ steps.bump.outputs.bump }}
