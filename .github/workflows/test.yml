name: Bump Patch test

on: workflow_dispatch

jobs:
  history:
    name: Bump
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Git Repository
        uses: actions/checkout@v3
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0
      - name: Bump
        id: bump
        uses: ./.github/actions/bump-version
      - run: echo ${{ steps.bump.outputs.bump_version }}
